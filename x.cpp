//code by Ahmed Adel Sayed ------ ID /20191491 #include <iostream> #include <string> using namespace std; struct mobile_shop { string Name_Item; int id; float price; int quantity; mobile_shop * next ; }; mobile_shop*top = NULL; //First Function(add item in shop ) void add_item (string Name_Item , int id , float price ,int quantity ) { mobile_shop * add_item = new mobile_shop ; add_item->Name_Item = Name_Item ; add_item->id =id; add_item->price=price; add_item->quantity=quantity; add_item->next=top; top = add_item; } //second Function(delete_item) void delete_item () { if (top == NULL) { cout <<"no items"<< endl; } else { cout << "\n"<<"""the deleted item is : "<<"\n"<< top->Name_Item<<" " <<top->id<<" "<<top->price<<" "<<top->quantity <<"\n"; top = top-> next; } } //third Function(search item if can available or not ) void search_item(string name) { mobile_shop* temp; if (top == NULL) cout << "no item in shop"; else { temp = top; while (temp != NULL) {
if (temp->Name_Item == name) { cout<<"The item is available " << "\n"; return; } temp = temp->next; } } cout<<"Item not found " <<"\n"; } //fourth Function(display quantity is zero ) void display_quantity_zero() { mobile_shop* temp; if (top == NULL) cout << "no item in shop"; else { temp = top; while (temp != NULL) { if (temp->quantity == 0) { cout << "\n"<<"The name of item quantity zero is : "; cout<<temp->Name_Item << "\n"; } temp = temp->next; } } } //fifth Function(display all items) void display_all_items() { mobile_shop* temp; if (top == NULL) cout << "no item in shop"; else { temp = top; cout << "\n"<<"The items in shop is : "<<"\n"; while (temp != NULL) { cout <<temp->Name_Item<<" "<<temp->price<<"$"<<" " <<temp->quantity<<"\n" ; temp = temp->next; } } } //sixth Function(main function) int main() { mobile_shop item; string name;
for (int x = 0; x < 4 ; x=x+1) { cout << "-------------------------------------------------"<<"\n"; cout <<"(Add items in mobile shop )"<<"\n"; cout << "-------------------------------------------------"<<"\n"; cout << "Enter Name of item: "<<"\n"; cin >> item.Name_Item; cout << "Enter ID of item: "<<"\n"; cin >> item.id; cout << "Enter price of item : "<<"\n"; cin >> item.price; cout << "Enter quantity of item : "<<"\n"; cin >> item.quantity; //call function (add_item) add_item(item.Name_Item, item.id, item.price, item.quantity); } //call function (display_all_items) display_all_items(); //call function (delete_item) delete_item(); //call function (search_item) cout<<"\n"<<"Enter name of item to search :"<<"\n"; cin>>name; search_item(name); //call function (display_quantity_zero) display_quantity_zero(); return 0; }
